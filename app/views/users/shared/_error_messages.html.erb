<% if resource.errors.any? %>
  <div id="error_explanation" data-turbo-cache="false" class="bg-red-100 text-lg text-red-700 px-5 py-5 mb-5 rounded-xl relative">
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li class="py-1">・<%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
