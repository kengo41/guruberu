<%= turbo_frame_tag bookmark_list do %>
  <div class="flex items-center justify-between">
    <p id="list_name" class="text-2xl"><%= bookmark_list.name %></p>
    <%= link_to t('defaults.edit'), edit_bookmark_list_path(bookmark_list), class: 'w-20 h-12 btn bg-blue-500 hover:bg-blue-600 text-white' %>
  </div>
<% end %>
